@if (!string.IsNullOrWhiteSpace(Message))
{
    <div class="alert @CssClass" role="alert">
        @Message
    </div>
}

@code {
    [Parameter] public string? Message { get; set; }
    [Parameter] public string Type { get; set; } = "success";

    private string CssClass => $"alert-{Type}";
}
